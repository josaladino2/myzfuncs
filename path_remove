path_remove () {
  # Usage: path_remove /path/to/remove
  if [[ -d "$1" ]]; then
    path=(${path[@]/$1/})  # Remove all instances (zsh array magic)
    export PATH="$path"
    echo "✗ Removed $1 from PATH"
	fi
}
